// 19. Monte um for que faça uma contagem regressiva de 10 até 0 e ao final 
// mostre "Fogo!". 

for (let num = 10; num >= 0; num--) {
    if (num === 0) {
        console.log("Fogo!");
    } else {
        console.log(num);
    }
}
